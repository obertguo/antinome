#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../youtube-user-downloader/youtube-user-downloader" "$@"
  ret=$?
else 
  node  "$basedir/../youtube-user-downloader/youtube-user-downloader" "$@"
  ret=$?
fi
exit $ret
